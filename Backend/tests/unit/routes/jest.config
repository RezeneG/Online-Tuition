// backend/jest.config.js
export default {
  // Test environment
  testEnvironment: 'node',
  
  // Files to run after test environment is set up
  setupFilesAfterEnv: ['<rootDir>/tests/setup.js'],
  
  // File extensions Jest will look for
  moduleFileExtensions: ['js', 'json'],
  
  // File patterns for tests
  testMatch: [
    '**/tests/**/*.test.js',
    '**/tests/**/*.spec.js',
    '**/__tests__/**/*.test.js'
  ],
  
  // Files to collect coverage from
  collectCoverageFrom: [
    'routes/**/*.js',
    'models/**/*.js',
    'middleware/**/*.js',
    'config/**/*.js',
    'scripts/**/*.js',
    '!**/node_modules/**',
    '!**/tests/**',
    '!**/coverage/**'
  ],
  
  // Coverage directory
  coverageDirectory: 'coverage',
  
  // Coverage reporters
  coverageReporters: [
    'text',
    'lcov',
    'html',
    'json'
  ],
  
  // Coverage thresholds (adjust as needed)
  coverageThreshold: {
    global: {
      branches: 70,
      functions: 70,
      lines: 70,
      statements: 70
    }
  },
  
  // Transform configuration (important for ES modules)
  transform: {},
  
  // Module name mapping (optional, for cleaner imports)
  moduleNameMapping: {
    '^@/(.*)$': '<rootDir>/$1'
  },
  
  // File extensions to ignore
  modulePathIgnorePatterns: [
    '<rootDir>/dist/',
    '<rootDir>/build/'
  ],
  
  // Test timeout (in milliseconds)
  testTimeout: 10000,
  
  // Verbose output
  verbose: true,
  
  // Color output
  colors: true,
  
  // Watch plugins
  watchPlugins: [
    'jest-watch-typeahead/filename',
    'jest-watch-typeahead/testname'
  ]
};
